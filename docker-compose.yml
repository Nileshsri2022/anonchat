version: '3'
services:
  user1:
    build: .
    ports:
      - "3001:3000"
    environment:
      - TOR_SOCKS_PORT=9050
      - TOR_CONTROL_PORT=9051

  user2:
    build: .
    ports:
      - "3002:3000"
    environment:
      - TOR_SOCKS_PORT=9060
      - TOR_CONTROL_PORT=9061

  user3:
    build: .
    ports:
      - "3003:3000"
    environment:
      - TOR_SOCKS_PORT=9070
      - TOR_CONTROL_PORT=9071
